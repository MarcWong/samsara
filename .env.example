# Supabase run telemetry (optional -- both unset disables it entirely).
# Baked into the bundle at build time; the anon key is public by design and
# can only INSERT into public.runs (see supabase/runs.sql).
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
